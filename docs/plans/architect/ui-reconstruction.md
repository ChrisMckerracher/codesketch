# Codesketch UI reconstruction: executable contracts

## Authority and outcome

Implement the final Gemini 3.8 Flash High creative package in `/private/tmp/codesketch-gemini38-ui-20260910`: `DESIGN_SYSTEM_AND_HIERARCHY.md`, `CREATIVE_COMPONENT_INTENTIONS.md`, `index.html`, and the seven `screenshot-state*.png` references. The earlier technical draft has been replaced by component intentions. The prototype is a visual reference; application behavior comes from retained code and `docs/ui-reconstruction-contracts.md`. This plan supplies final technical bindings for illustrative inaccuracies without another creative loop.

Preserve Gemini's five-zone arrangement: document header, left Layers/Feedback sidebar, central artwork stage, bottom floating tool dock, and right contextual inspector; the floating playback HUD remains at the top of the stage. Gemini numbers the stage HUD separately as Zone 5; that numbering does not change the arrangement. Use its proportions, spacing, tokens, neutral surfaces, and layer-return screenshot. At compact widths retain the proposed sidebar rails/toggles and fitted canvas. Supply light appearance and reduced-motion styling within this same hierarchy.

The lead delegates through Herdr to OpenCode, reviews, and integrates. Workers own only listed files, ship standard-library/browser APIs, keep files preferably under 200 and always at most 300 lines, install nothing, and make no commits or production changes. Tasks target 300–500 changed lines including tests; split before 1000. H1 opacity work is already assigned and exclusively owns `studio/state.mjs` and its existing unit test. Do not duplicate it. No production port, actual artwork, or persistent staging runtime participates.

## Initial delegation packet

Start the replacement UI now with **U1 shell**, **U2 model/local actions**, and then **U3 header/HUD**, **U4 layers**, **U5 contextual inspector**, **U6 dock/viewport** against the fixed contracts below. These presentation modules can be authored from injected model values and dispatch spies while the network tasks proceed. They must land with real application bindings through U11 before completion. No additional incidental hazard investigation gates this sequence.

- **U1 handoff:** own only `public/index.html`, `tokens.css`, `workspace.css`; reproduce the final arrangement and exact shared mounts, light/dark surfaces, compact reveal wrappers, and CSS sizing. Supply actual semantic buttons for compact reveal/close controls, no prototype fixtures or application handlers. Leave each feature mount ready for its named module. Static shell links to pending sibling assets are integration dependencies, not reasons to invent placeholders in other owners' files.
- **U2 handoff:** own only `studio/model/**`, `application/local.mjs`, and `studio-model.test.mjs`; implement the model and local dispatch contract, including layer-to-brush return and compact open state. All intents are objects with a `type` equal to the table's intent name and payload fields at top level. Export `handleLocal(intent,{state,model}) -> boolean`; true means handled locally. No fetch or DOM imports. Verify target preservation, context switching, and parameter events with injected state.
- **U3–U6 handoff:** use `mount({root,model,dispatch})` where `model` is the current immutable value, and the returned `update(nextModel)` receives subsequent values. The application owns subscription fan-out; components own only event listeners inside their mounts. Buttons have accessible names matching their real actions. Each component can be reviewed with an injected dispatch spy; no mock application state ships.
- **First review:** shell proportions and named mount presence; selecting Brush exposes its properties; selecting a named layer exposes layer settings; return pill/B returns to Brush and preserves target. Follow with real pointer/network integration. Full feature/task ownership remains in the assignment table.

Gemini's finalized light/compact addendum is now present in `CREATIVE_COMPONENT_INTENTIONS.md` sections 4–5, with `screenshot-light-desktop.png` and `screenshot-compact-properties-open.png`. Those references govern U1/U6 appearance and reveal behavior. The technical capability labels in this plan remain authoritative.

**Bounded state cleanup S1 (approximately 30–60 lines):** assign to the H1 owner or sequentially after H1; exclusive files are `S/state.mjs` and `T/studio-state.test.mjs`. Read only top-level `snapshot.playbackError` and remove the obsolete `snapshot.playback?.playbackError` fallback. Accept the current top-level string/null contract; do not interpret nested-only input as an error source. Add focused assertions for top-level error, explicit null clearing current error state, and nested-only input providing no fallback. `Session.snapshot()` already publishes the top-level field. No domain/HTTP/Go changes or compatibility adapter. S1 proceeds alongside shell work.

## Final capability bindings

| Creative intention | Executable behavior |
| --- | --- |
| Choose brush, inspect layer, return to brush | Drawing-tool selection opens its properties immediately. Clicking a layer selects its ID and opens layer properties. The target-layer Inspect button opens that same context. The explicit return pill and B select brush/open brush properties while retaining target ID and parameters. A canvas pointerdown while inspecting a layer returns to brush and starts the normal guarded paint gesture; no extra click is consumed. Comment/pan gestures never trigger this return. |
| Tool properties | Brushes are brush/pencil/marker/eraser; size 1–100, opacity 0–1, six-digit hex color. Presets 2/8/16/24/48 and recent swatches populate local parameters. Filled rectangle/ellipse use dragged bounds, fill color, opacity, target layer. Display dimensions for the current shape draft; no outline properties. Eraser is a layer-local stroke. Ring is a nominal size guide, not an exact soft-brush pixel boundary. |
| Layers | Render last document layer first in the list to show the topmost composite first. Add unique valid IDs, rename, select, visibility, and opacity including zero. Keep focused controls/held sliders stable across incoming snapshots. Commit layer opacity on change/release; local editing is labelled pending until accepted. Hidden target is identified beside painting parameters, with an explicit Show action. Do not silently unhide it. |
| View navigation | Implement Fit, 100%, bounded zoom 25–400%, and Hand drag as local view state. Fit may go below 25% when necessary to preserve the whole artboard. Use H for Hand; Space exclusively controls playback. No document mutation from pan/zoom. Overlay shares the canvas transform. |
| Playback HUD | Render `Idle`, `Playing · Nx`, `Pausing…`, or `Paused`, plus `N commands remaining`. A progress track can display the current active command fraction only, labelled as such; hide it when no active command exists. No invented batch denominator or agent identity. Speed choices 0.5/1/2/5 are valid. Pause/Resume, Step, Clear Pending, Finish All invoke server controls. Empty finish remains a no-op; clear and finish remove active preview as defined by the domain. |
| Human authority | Show pause pending until an accepted current acknowledgement. Confirmed pause revokes execution authorization. Cancel/reselection/reconnect never resumes. Resume is an explicit action. Stage-only agent submissions can arrive while paused; grant checks prevent unauthorized execution. UI labels do not claim that all agent submission is blocked. |
| Feedback | Display `Open`, `Acknowledged`, `Addressed`, `Resolved`. Reopen yields Open. Resolve is available only for Addressed; Reopen for Acknowledged/Addressed/Resolved. Show number, timestamp, text, region/whole canvas; omit invented authorship. Separate `Last agent check: …` from lifecycle; heartbeat proves a past poll only. |
| Feedback composer | Comment mode obtains pause acknowledgement before region selection. Offer Region and Whole Canvas within the same review interaction; whole canvas sends `rect:null`. Checked `Keep paused` sends `continuePlayback:false`; unchecked explicitly states `Discard pending work and authorize agent` and sends true. True grants execution and clears active/queued work while status remains paused awaiting agent action. Cancel preserves pause. |
| Header identity/status | Editable title is a local download filename only; label accessibly as Filename and initialize Untitled. No project metadata extension. Status displays Connecting, Connected, Sending…, Changes applied, Offline, or the actual storage/playback error. Acknowledged writes are not a persistence guarantee. No fabricated recovery count/log or “Saved locally” badge. A new instance refreshes context and can announce `Session refreshed`; preserve unsent text for review. |
| Projects and export | New, Example, Open v2 JSON, Save v2 JSON, Undo/Redo. Open rejects oversized input before read (>8 MiB), validates via server, and replaces only on success. Save fetches the server project; preserves its exact v2 shape. Export PNG downloads committed artwork at 1000×700 using a separate renderer with no active/draft/overlay. Explicit accessible description: committed artwork. Native CLI crop/scale remains available through the CLI; no browser-to-CLI endpoint or new capture protocol is needed. |

Omit the prototype state navigator/reviewer controls, fake artwork/threads/counters, object Select/Move, duplicate/delete/reorder/lock-layer controls, shape stroke controls, decorative overflow menus without actions, draggable-looking resize handles unless implemented, native-export options without browser backing, merge/recovery-log actions, and unconditional listening/author badges. Keep the remaining navigation/drawing/shape/comment groups in their authored order. Region reselection uses a real Reselect action; the selection border has no inert handles. Do not import prototype inline scripts/styles, global handlers, or HTML wholesale.

## Shared DOM and module interfaces

Paths abbreviated below: S=`apps/studio/src/studio`, P=`apps/studio/public`, T=`apps/studio/tests`, B=`T/browser`. Each nested JS context exposes `index.mjs`; consumers use that entrypoint. CSS files are linked explicitly from HTML, with no inline handlers or CSP weakening.

Shell owns exactly these stable mounts: `#studio-app`, `#global-header`, `#left-sidebar`, `#layer-panel`, `#feedback-panel`, `#stage-viewport`, `#canvas-wrapper`, `#painting-canvas` (1000×700), `#stage-overlay` (SVG viewBox 0 0 1000 700), `#feedback-composer`, `#director-hud`, `#tool-dock`, `#right-inspector`, and `#studio-notice` (`role=status`, polite). Header/sidebar containers are authored once; each feature owns only its mount descendants. The viewport controller owns canvas-wrapper sizing/transform. Review owns overlay selection/pins; gesture owns a separate SVG group `data-overlay="brush"`. No feature replaces the canvas or another feature's mount.

Compact shell adds wrappers `#compact-layers-drawer` around the existing layer/feedback mounts and `#compact-inspector-drawer` around the existing inspector mount; these are the same controls, not duplicate compact forms. U1 supplies rail triggers with `data-drawer-open="left|right"`, close buttons with `data-drawer-close="left|right"`, `aria-controls` and `aria-expanded`. U6 owns their reveal/close event handling and dispatches `drawer.set {side:'left'|'right',open:boolean}`; U2 stores `drawers:{left:boolean,right:boolean}` in the local model. Left rail is 52px with Layers/Feedback triggers; right rail is 56px with Properties/color triggers; reveal widths are 260/280px per Gemini. Left tab intent selects the tab and opens the drawer; B/tool selection exposes corresponding properties in compact mode. L opens Layers, C begins review and reveals Feedback; no conflicting C-only tab toggle.

Escape/close returns focus to the opener, or canvas if the opener is unavailable. Canvas pointerdown closes open drawers without resetting values and continues through the normal gesture contract. Flyouts are nonmodal, keyboard reachable, scroll internally when needed, and retain focus while editing. HUD pause, tool dock, file actions, and drawer-close controls must remain reachable at 1024px and short windows: wrap/scroll within their existing zones, preserve visible pause, and never rely on clipped overflow. Both drawers can be opened as shown in Gemini's addendum. V3 checks named-layer selection, full parameter editing, return path, and all actions in both light/dark compact states.

All UI contexts export `mount({root,model,dispatch}) -> {update(model),destroy()}`. They create descendants once, reconcile by stable layer/comment ID, and patch values without replacing focused elements. `dispatch(intent)` returns a Promise for accepted mutation completion or local completion, rejects on failure, and is the sole feature-to-command path. Features handle pending/disabled state and retain local edits on rejection. They never call fetch. Local UI state belongs to `S/model/index.mjs`, server snapshots remain in `StudioState`.

`model` is an immutable view value `{snapshot,tool,size,opacity,color,targetLayer,draft,context,tab,filename,connection,pending,notice,review,viewport}`. `context` is `document|tool|layer`; `tab` is `layers|feedback`; `connection` is `connecting|online|offline|uncertain`; `pending` lists operation keys. `review` is `{phase,rect,text,keepPaused,requestId,generation,artRevision}` with phases `closed|pausing|selecting|composing|submitting|stale|uncertain`. `viewport` is `{mode:'fit'|'manual',scale,x,y}`. `createModel(state)` exposes `get()`, `patch(localFields)`, `subscribe(fn)`, `destroy()`; it subscribes to StudioState events and prohibits patching the server snapshot. Only the central session coordinator applies snapshots. Subscriptions return disposers.

| Intent and payload | Owner/binding |
| --- | --- |
| `tool.select {tool}`, `tool.properties {size?,opacity?,color?}`, `layer.inspect {id}`, `tool.return`, `tab.select {tab}`, `filename.set {value}` | `S/application/local.mjs`; sets model and retained setters. Tool values include drawing tools, rect, ellipse, hand, comment. Comment selection delegates to review service. |
| `layer.add {name}`, `layer.update {id,name?,visible?,opacity?}`, `background.set {color}`, `stroke.commit {command,generation}` | `S/application/mutations.mjs`; captures current generation for click actions, respects supplied gesture generation, emits immediate human commands. Pending key prevents duplicate dispatch. |
| `playback.control {action,speed?}`, `document.new`, `document.demo`, `project.open {file}`, `project.save`, `png.export` | `S/application/actions.mjs`; routes session controls/document service. Pause has priority over unsent mutations. Generation is frozen at intent time. |
| `view.fit`, `view.actual`, `view.zoom {scale}`, `view.pan {x,y}` | Viewport context, local only. |
| `review.begin {scope:'region'|'whole'}`, `review.rect {rect}`, `review.text {text}`, `review.hold {value}`, `review.reselect`, `review.cancel`, `review.submit`, `review.retry`, `review.transition {id,reopen}` | Review service owns handshake and immutable request context; transition takes current item's sequence at click. |

`S/requests/index.mjs` exposes the bounded `StudioRequests` contract in H3: `readState`, `mutate({expectedDocGeneration,run})`, `observe`, `invalidate`. Add `pause({expectedDocGeneration})` explicitly: cancels unsent ordinary intents, awaits outstanding request settlement, sends human pause with the captured generation, and resolves only its accepted current paused response. Generation rotation rejects; refreshing never silently rebinds pause to a different document. An uncertain outstanding mutation permits a fresh read and pause for the same confirmed generation, with the uncertain write still reported. No indefinite pause queue behind automatic retries.

`S/application/index.mjs` exports `createApplication({state,api}) -> {model,dispatch,requests,destroy}`. It composes focused local/mutation/action handlers and injected review/document/viewport services through their public entrypoints. No feature imports another feature's private file. Entrypoint constructs application and mounts features, then starts one coalesced 100ms state polling loop (retry 1000ms after failure); stops timers/listeners on teardown. Revisions and instance IDs gate snapshots; generation rotation cancels drafts/selection activation and preserves review text as stale. Equal unchanged envelopes update heartbeat only. Render on accepted artwork/active change plus local draft changes; control-only JSON object identity does not require rebuilding artwork buffers.

## Safety and gesture contracts

Use H2 response validation/deadline plus H3 request sequencing for the rebuilt client. All requests keep the 10s deadline through body read, reject malformed endpoint responses, preserve HTTP errors, and sequence connectivity effects. A mutation timeout/network/protocol/flush failure is uncertain; do not retry unkeyed writes automatically. On 409 refresh and preserve user work for renewed intent. Safe comments may retry the exact frozen request ID/payload. A refresh cannot prove an unkeyed stroke's execution history.

Integrate H4/H5/H6 atomically: human HTTP commands/control/project/demo require exact top-level `expectedDocGeneration`; check after full request-body read immediately before mutation, return 409 without mutation on missing/stale context. Use H5's object-form API signatures; no contextless fallback. Agent epoch/grant/CLI contracts remain intact. This guard protects replacement documents; local serialization orders local intents. Same-generation cross-tab human control ordering remains a known protocol limit; UI must not claim global tab locking. Trusted in-process session and recovery APIs retain their current behavior.

Pointerdown freezes document generation, target, tool and parameters; requests pause and captures events locally while awaiting acknowledgement. A local preview may show pending input; no command commits before accepted pause. Pointerup before acknowledgement retains the bounded completed gesture until acknowledgement. Pointercancel/lost capture cancels the gesture; generation rotation invalidates it. Block a second paint gesture while its commit is pending. Limit strokes to 2000 points by bounded sampling with final endpoint preserved; never split one gesture into surprise undo steps. Clamp via `canvasPoint` with transformed canvas bounds. Shapes use positive normalized in-bounds dimensions; zero-area shapes do not commit. Draft and accepted pixels must agree, with local draft removed only once accepted artwork contains the successful commit; uncertain writes clear the visual draft after refresh and retain an explicit uncertainty notice, preventing double-rendering.

Layer name/opacity controls retain their local edit while focused/held; apply on change/blur once, using frozen layer/generation. Snapshot layer disappearance cancels the edit. Native keyboard activation works without pointerdown. Brush parameters update immediately but a held stroke retains frozen parameters. Single-key shortcuts ignore editable targets and composition, use B/Shift+P/M/E/R/O/C/H, Space playback, ] step; Cmd/Ctrl+Z, Shift+Z, S for history/save. Space on a focused button/input preserves native behavior; Hand never borrows Space. Canvas Enter does not invent a paint gesture.

Review: PauseHandshake predicate checks token, accepted same-generation snapshot, and paused state. Expire on cancel/reset/replacement. Freeze artRevision at reviewed selection; artwork changes cause stale review with text retained and submission disabled until explicit reselection. Retrying uncertain submission keeps the same requestId/text/rect/continuation/artRevision. New payload creates a new request ID. Map exact comment transitions and retain errors. Opening a stored pin highlights its region in current artwork; it does not restore a historical canvas. Whole-canvas and region review share the same composer/authorization rules.

## Assignable implementation units

Rows define exclusive file ownership. New files below an owned directory belong to that row only; a later row may touch an earlier file only where explicitly sequenced. Tests listed are implementation-owned; the lead runs and reviews final gates. Each row is a separate small OpenCode assignment, not permission to expand architecture.

| ID / target lines | Owned files | Contract, dependency, focused validation |
| --- | --- | --- |
| N1 / 350–500 | S/response.mjs; T/studio-api-response.test.mjs | H2 endpoint validators, exported validation function `(kind,value,{allowUnchanged})`; current envelopes and malformed nested data. Independent. |
| N2 / 350–500 | S/api.mjs; T/studio-api-deadline.test.mjs; T/studio-api-human-context.test.mjs | After N1: H2 full-body timeout and terminal connectivity ordering; H5 required generation/object signatures in the same edit. Mock fetch checks deadlines, cancellation, exact bodies, invalid JSON/HTTP status, no premature reconnect. |
| N3 / 400–500 | S/requests/**; T/studio-request-order.test.mjs | After N2: H3 plus explicit priority pause above. Deferred responses prove polling coalescing, stale generation, uncertainty/no replay, pause supersession and restart. |
| G1 / 300–450 | apps/studio/src/transport/server.mjs; transport/human-context.mjs; T/human-generation-http.test.mjs; transport/README.md | H4 guard; delayed request body across reset, all guarded routes, exact rejection atomicity, agent unchanged. Parallel with N1–3; integrate only with N2/G2/G3. |
| G2 / 250–450 | T/http.test.mjs; comments-http.test.mjs; comments-reset-http.test.mjs; finish-http.test.mjs; lifecycle.test.mjs | H5 fixture generations from observed snapshots. Replace root/entrypoint reset-only 404 assertions with R1's serving contract, retaining forbidden-path checks. Preserve negative cases and raw helpers. After G1/R1. Browser fixtures belong to V rows. |
| G3 / 150–300 | apps/paint/internal/cli/studio_test.go; comments_studio_test.go; lifecycle/restart_test.go; restart_concurrent_test.go; stop_failure_test.go | H6 human seed generations. CLI agent payloads/capture fixtures untouched. After G1; affected Go tests with networking disabled. |
| U1 / 400–500 | P/index.html; P/tokens.css; P/workspace.css | Creative shell and named mounts; explicit links to all planned feature styles and module entrypoint; light/dark tokens, geometry, responsive rails. No fake content. Visual comparison against final brush/layer/compact references. Independent. |
| U2 / 300–450 | S/model/**; S/application/local.mjs; T/studio-model.test.mjs | Shared immutable model/local intents; brush→layer→brush path, stable target and parameter values, reset invalidation. Consume H1; no edits to state.mjs. |
| U3 / 350–500 | S/header/**; S/playback/**; P/controls.css | Mount header/HUD, local filename/menu, actual remaining/fraction/status, safe pending states and action intents. After U1/shared contracts; native accessible controls. No network. |
| U4 / 350–500 | S/layers/**; P/layers.css | Left tabs/layer rows, add/select/visibility/rename; stable keyed reconciliation. Emit layer intents; preserve keyboard focus. After U1/U2; validate selection and rename/hidden state in component harness. |
| U5 / 350–500 | S/inspector/**; P/inspector.css | Brush/shape/layer/document contexts, return pill, presets/swatches, zero opacity, name/visibility/opacity editing. After U2, U4 contract. Test held input across model updates through later browser checks. |
| U6 / 300–450 | S/viewport/**; S/tools/**; P/stage.css | View Fit/100%/zoom/Hand transforms; dock buttons and key routing; separate overlay group brush guide. Expose viewport mount plus `point(event)` and `fit()`; same coordinate transform for gesture/review. After U1/U2. Unit geometry where useful; browser fit/pan checks V1. |
| U7 / 350–500 | S/gesture/**; T/studio-gesture.test.mjs | After N3/U2/U6: pointer ownership, frozen bounded stroke/shape drafts, async pause-before-commit, release/cancel/reset. Inject `{model,dispatch,requests,canvas,point}`; own listeners/brush SVG group only. Deferred pause unit checks plus V1 real pixels. |
| U8 / 350–500 | S/review/index.mjs; S/review/session.mjs; T/studio-review.test.mjs | After N3/U2: review state machine and retained PauseHandshake integration, exact submission/retry/seq context. Public `createReview({model,requests,dispatch}) -> {handle(intent),destroy}`. No presentation. Unit races including art-change reselection and duplicate uncertain request. |
| U9 / 350–500 | S/review/presentation/index.mjs; S/review/presentation/view.mjs; S/review/presentation/overlay.mjs; P/feedback.css | After U8: feedback list/composer/overlay DOM and selection pointer handling, whole/region scope, explicit continuation consequence, accurate lifecycle. U8 index predeclares the re-export of mount from this public presentation entrypoint; U9 does not edit U8 files. V2 covers actual interaction. |
| U10 / 300–450 | S/documents/**; T/studio-documents.test.mjs | After N3: file input/read cap, server project Save/Open, New/Example, committed PNG offscreen render/download with URL cleanup. Export `createDocuments({model,requests}) -> {handle(intent),destroy}`. No format metadata; test invalid file/uncertain load and separate export surface. |
| U11 / 350–500 | S/application/index.mjs; S/application/mutations.mjs; S/application/actions.mjs; S/index.mjs; S/README.md | After N3/U1–U10: compose services/dispatch; mount all features, poll, accept/reset snapshots, render document+active+draft, teardown; notices and promise handling. No feature DOM duplication. Smoke test page errors then V suites. |
| R1 / 250–400 | apps/studio/src/transport/http.mjs; T/static-ui.test.mjs; tests/studio-dev.test.mjs | After U1: GET `/`→public/index.html, `/public/*.css` from explicit stylesheet filename list, retained allowed module paths. Realpath containment, MIME, 404 traversal/symlink, CSP preserved. Update reset-only 404 expectations in developer smoke tests. No changes to port defaults. |
| R2 / 200–350 | apps/studio/assets.go; assets_test.go; tests/runtime-policy.test.mjs | After all runtime files finalized: explicit embeds for every canonical source/public asset, replace removed-UI expectations with current entrypoint/static inventory tests. Preserve excluded tests/docs/art/state and renderer identity. Go asset/policy tests and extracted-runtime startup off production. |
| V1 / 350–500 | B/studio.mjs; B/layers-keyboard.mjs; B/layers-opacity.mjs | After U1–11/R1/protocol integration: real brush/shape pixels, all tool parameters, zero opacity, eraser/layers, return pill/B/canvas route, focused/held controls, fit/zoom/Hand, history, Save/Open, committed PNG. Generation-correct raw setup. New focused scenario files if needed within ownership/line ceilings. |
| V2 / 350–500 | B/comments.mjs; B/comments-races.mjs; B/finish.mjs | Real paused region/whole composer, cancel/reselect, lost response duplicate retry, grant/resolve/reopen, stale art/late pause/generation reset; progressive remaining/step/finish/clear and independently undoable marks. Compare server snapshots and pixels. |
| V3 / 250–450 | B/connection.mjs; B/appearance.mjs; T/browser/README.md | Offline/500 uncertainty, delayed old response, restart while draft/selection held; 1440 and 1024 widths, light/dark, keyboard focus, reduced motion and no blocked controls. Use browser routing or ephemeral server fixtures only. |
| R3 / 200–350 | tools/browser-check.mjs; tools/browser-artifacts.mjs if extraction needed; docs/verification.md; apps/studio/README.md | After V1–3/R2: remove suspension, register all current scenarios, keep unique browser session/ephemeral server and cleanup, report failure honestly. Restore artifact verification for new Save/PNG filenames. Record actual results; update UI availability docs. Lead runs full gates. |

U8's predeclared presentation export is integrated with U9 before entrypoint startup. All style ownership remains exclusive; shell U1 links `tokens.css`, `workspace.css`, `controls.css`, `layers.css`, `inspector.css`, `stage.css`, `feedback.css` in that order. Feature selectors are scoped to their mounts; shared button/input appearance belongs to controls.css. R1 serves `/` as the HTML entrypoint and `/public/index.html` as the same explicitly allowed file; all other public access is restricted to the seven named stylesheets. G2 updates both root and entrypoint assertions to match.

N2/G1/G2/G3, browser raw fixture changes in V1/V2, and R2 inventory form one current-contract integration set before final green status. Branch work may temporarily depend on pending siblings; no worker ships a contextless compatibility path to make an intermediate check green. The manager may assign independent U1/U2/N1/G1 immediately and serialize shared api/inventory work through the dependencies above.

## Completion evidence

Manager runs `npm run verify`, `npm run test:browser`, and `PAINT_STUDIO_TESTS=1 PAINT_BROWSER_TESTS=1 make test-go` with existing installed tooling, ephemeral loopback and disposable data. Inspect browser console/network/CSP, PNG pixels and dimensions, exact project v2 roundtrip, managed extracted-runtime entrypoint, and renderer fixture identity. Current reset-only tests must be replaced where they conflict with the newly restored surface; do not disable coverage. `tools/studio-dev.mjs` already safely defaults to port 0; preserve it.

Review screenshots of brush, layer inspection/return, live playback, paused review, empty document, actual connection/error state, compact layout, and light appearance against Gemini's final arrangement. Exercise all visible controls; omit any remaining nonfunctional decoration. Verify no retired instance replaces current artwork; no changed-generation gesture commits; no comment submit bypasses accepted pause; no request error silently duplicates a mutation; no save badge promises unsupported persistence. Known same-generation cross-tab ordering limit is documented rather than concealed.

This plan was prepared from final correct-model creative files and retained code/standards only. No application code, tests, dependencies, commits, agents, or production runtime were changed during authorship.
